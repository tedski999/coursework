#include "skybox.h"

void cbi_skybox_render() {
	/*
	glUseProgram(cbi->skybox_shader->id);
	glActiveTexture(GL_TEXTURE0);
	glBindTexture(GL_TEXTURE_2D, cbi->cb.skybox[0]->id);
	glUniformMatrix4fv(cbi->skybox_shader->u_transform, 1, GL_FALSE, &mvp[0][0]);
	glBindVertexArray(cbi->skybox_vao);
	glDrawElements(GL_TRIANGLES, 16, GL_UNSIGNED_INT, 0);
	*/
}
